# TigerMed self-hosted desktop build.
#
# electron-vite inlines these public URLs into the renderer bundle when built
# with `--mode tigermed`.

VITE_API_URL=https://aicoding.tigermed.net
VITE_WS_URL=wss://aicoding.tigermed.net/ws
VITE_APP_URL=https://aicoding.tigermed.net
